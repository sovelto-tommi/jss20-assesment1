const Vari = require('../vari')
const VariLista = require('../varilista')

// Nämä kaksi mukaan testeihin, eli kommentit pois
test("Varilistan luonti onnistuu", () => {
    // const lista = new VariLista() 
    // // lista.varit: pitäisi olla JavaScriptin Array-tyyppiä
    // expect(lista.varit.length).toBe(0)
})

test("Värilistalle lisääminen onnistuu", () => {
    // let vari = new Vari("oranssi", "#ffa500", [255,165,0])
    // const lista = new VariLista();
    // lista.lisaaVari(vari)
    // expect(lista.varit.length).toBe(1)
    // expect(lista.varit[0].nimi).toMatch("oranssi")
})